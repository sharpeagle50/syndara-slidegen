"""Syndara deck-generation engine (open source, MIT).

Self-contained pipeline that turns a topic/outline into a PowerPoint deck and
redesigns existing decks. Has no dependencies on Syndara's private code — the
private orchestration layer (web_runner, storage, guide) imports *into* this
package, never the other way around.
"""
